const express = require("express");
const app = express();

// define middlewares
app.use("'/api/alexa", )

port = 3000;
app.listen(port, () => {
  console.log(`Port is currently running on ${port}`);
});
